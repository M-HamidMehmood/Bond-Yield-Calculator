# Bond Yield Calculator — Frontend

React + Vite + TypeScript UI for the [Bond Yield Calculator](../../README.md). Install and run from this folder:

```bash
npm install
npm run dev
```

Then open http://localhost:5173. See the **root [README](../../README.md)** for full setup, API, and project structure.
